meta {
  name: Me con token
  type: http
  seq: 6
}

get {
  url: http://localhost:3000/api/auth/me
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.rol: isDefined
}
