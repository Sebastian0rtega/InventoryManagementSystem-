meta {
  name: Listar ventas -> 200
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/sales?page=1&limit=20
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
