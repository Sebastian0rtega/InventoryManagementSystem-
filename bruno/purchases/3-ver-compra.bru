meta {
  name: Ver compra por id -> 200
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/purchases/{{compraId}}
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}
