meta {
  name: Listar categorías (autenticado)
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/categories
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
