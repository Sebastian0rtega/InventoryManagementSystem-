meta {
  name: Eliminar categoría con productos asociados → 409
  type: http
  seq: 9
}

delete {
  url: http://localhost:3000/api/categories/1
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 409
}
